{
  "name": "cli-agent",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "cli-agent": "./src/cli.js"
  },
  "scripts": {
    "test": "vitest run --exclude test/integration.test.js",
    "test:watch": "vitest",
    "test:integration": "OPENROUTER_RUN_INTEGRATION=1 vitest run test/integration.test.js",
    "dev": "node src/cli.js"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.5.2"
  }
}
